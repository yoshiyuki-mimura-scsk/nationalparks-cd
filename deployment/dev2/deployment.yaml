apiVersion: apps/v1
kind: Deployment
metadata:
  name: nationalparks2
  namespace: scsk-cd-dev
  labels:
    app: nationalparks2
    app.kubernetes.io/component: nationalparks2
    app.kubernetes.io/instance: nationalparks2
    app.kubernetes.io/name: nationalparks2
    app.kubernetes.io/part-of: nationalparks-git-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nationalparks2
  template:
    metadata:
      labels:
        app: nationalparks2
        deployment: nationalparks2
    spec:
      containers:
        - name: nationalparks2
          image: image-registry.openshift-image-registry.svc:5000/scsk-cd-dev/nationalparks:latest
          ports:
            - containerPort: 8080
              protocol: TCP
            - containerPort: 8443
              protocol: TCP
            - containerPort: 8778
              protocol: TCP
